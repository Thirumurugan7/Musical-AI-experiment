# audio is not redistributable and is large
*.wav
*.mp3
*.m4a
*.flac

__pycache__/
*.py[cod]
.venv/

# scratch workspace when CHORDSTRUM_ROOT points inside the repo:
# ChordMini clone, demucs venv, separated stems, per-song audio
work/

# scratch: benchmark audio and derived artifacts (large, not redistributable)
bench/audio/
work/strumtests/
work/refs/

# secrets. .venv/ above is the virtualenv and does not cover these: nothing in
# this file matched .env at all, so a dotenv dropped at the repo root would
# have been staged without complaint.
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json
